export const cmdOverloaded =
  "This command does not require any additional arguments, so they are ignored\n";
export const lackOfArgs =
  "This command require additional arguments, but they are absent\n";
